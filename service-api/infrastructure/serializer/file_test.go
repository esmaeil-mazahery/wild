package serializer_test

import (
	"testing"
)

func TestFileSerializer(t *testing.T) {
	t.Parallel()

	// binaryFile := "../tmp/serializer-file-test.bin"
	// jsonFile := "../tmp/serializer-json-test.json"

	// member1 := &pb.Member{Email: "aaaaaa", Id: "sssssss"}
	// err := serializer.WriteProtobufToBinaryFile(member1, binaryFile)
	// require.NoError(t, err)

	// member2 := &pb.Member{}
	// err = serializer.ReadProtobufFromBinaryFile(binaryFile, member2)

	// require.NoError(t, err)
	// require.True(t, proto.Equal(member1, member2))

	// err = serializer.WriteProtobufToJSONFile(member1, jsonFile)
	// require.NoError(t, err)

}
